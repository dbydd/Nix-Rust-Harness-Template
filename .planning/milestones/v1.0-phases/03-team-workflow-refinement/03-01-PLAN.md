---
phase: 03-team-workflow-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agent/skills/rust-feature.md
  - .agent/skills/bugfix.md
autonomous: true
requirements: [D-01, D-02]
user_setup: []

must_haves:
  truths:
    - "Agents can follow a structured feature workflow with concrete commands and verification criteria"
    - "Agents can follow a structured debugging workflow with reproduction and root cause phases"
    - "Both skills match the quality and specificity of polished skills"
  artifacts:
    - path: ".agent/skills/rust-feature.md"
      provides: "Rust feature workflow guide"
      min_lines: 80
      contains: "## Workflow"
      contains_2: "## Guardrails"
    - path: ".agent/skills/bugfix.md"
      provides: "Bug fix workflow guide"
      min_lines: 70
      contains: "## Workflow"
      contains_2: "## Guardrails"
  key_links:
    - from: ".agent/skills/rust-feature.md"
      to: "justfile"
      via: "`just check` and `just test` commands"
      pattern: "just check|just test"
    - from: ".agent/skills/bugfix.md"
      to: "justfile"
      via: "`just check` and `just test` verification commands"
      pattern: "just check|just test"
---

<objective>
Expand `rust-feature.md` and `bugfix.md` skills to match the depth, structure, and specificity of polished skills (`crate-discovery.md`, `nix-maintenance.md`).

Purpose: Remove inconsistency between lightweight stubs and mature skills by providing agents with actionable,command-driven workflows.

Output: Two expanded skill files with phased workflows, concrete commands, evaluation checklists, and explicit guardrails.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-team-workflow-refinement/03-CONTEXT.md
@.planning/phases/03-team-workflow-refinement/03-RESEARCH.md

<!-- Reference examples of polished skill structure -->
@.agent/skills/crate-discovery.md
@.agent/skills/nix-maintenance.md

<!-- Current lightweight skills to expand -->
@.agent/skills/rust-feature.md
@.agent/skills/bugfix.md

<!-- Verification commands reference -->
@justfile
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From .agent/skills/crate-discovery.md (reference pattern):
```markdown
## Goals
- [3-5 specific objectives]

## Workflow
### [Phase Name]
1. [Action with command]:
   ```bash
   command
   ```
   **What this checks/proves:**
   - [Verification criteria]

## [Category] Checklist
Before [action]:
- [ ] [Checklist item]

## Guardrails
- Do not [anti-pattern 1].
- Do not [anti-pattern 2].
```

From .agent/skills/nix-maintenance.md (reference pattern):
```markdown
## Workflow
### [Phase Name]
2. **Step N: [Action name]**
   ```bash
   command
   ```
   **Must:** Exit with code 0

   **What this proves:**
   - [Verification criteria]
```

From justfile (verification commands):
- `just check`: cargo fmt --check, cargo clippy -D warnings, cargo check
- `just test`: cargo nextest run--all-features
- `just ci`: check + test
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand rust-feature.md skill to polished pattern</name>
  <files>.agent/skills/rust-feature.md</files>
  <read_first>
    - .agent/skills/rust-feature.md (current content to expand)
    - .agent/skills/crate-discovery.md (reference pattern for structure)
    - .agent/skills/nix-maintenance.md (reference pattern for commands/checklists)
    - justfile (verification commands to reference)
    - AGENTS.md (verification contract for feature validation)
  </read_first>
  <action>
Expand `.agent/skills/rust-feature.md` from ~22 lines to ~80+ lines following the polished skill pattern from D-02 and RESEARCH.md.

The expanded skill must include:

1. **Goals section** (expand from 3 to 4-5 objectives):
   - Keep the feature small and testable
   - Preserve crate structure clarity
   - Add validation with the feature, not later
   - Maintain code quality boundaries
   - Document intent changes when workflow changes

2. **Workflow section** structured into phases:
   - **Before Implementation phase**: Define feature boundary (git diff --stat), identify existing patterns (rg -l), check for duplicates
   - **Implementation Phase**: Implement smallest complete version (cargo check --all-targets), add/update tests (cargo nextest run), verify compilation
   - **Verification Phase**: Run verification contract (just check && just test), confirm quality bar

3. **Each workflow step must include**:
   - Concrete command with expected output
   - **What this checks/proves:** annotation explaining verification purpose
   - **Must:** or **Expected:** success criteria

4. **Feature Checklist section** with categories:
   - Scope checklist (feature boundary, no unrelated refactors, narrowest module)
   - Testing checklist (tests added, edge cases, existing tests pass)
   - Quality checklist (just check passes, just test passes, no new clippy warnings, code follows patterns)
   - Documentation checklist (public API documented, README updated if workflow changed, STATE.md updated if intent changed)

5. **Guardrails section** (expand from 2 to 5-6 items):
   - Do not mix unrelated refactors with the feature.
   - Do not weaken lint rules to make the change pass.
   - Do not skip tests for new public interfaces.
   - Do not add dependencies without crate-discovery skill evaluation.
   - Do not change module structure without documenting rationale.
   - Do not bypass the verification contract.

Follow the exact structure from RESEARCH.md Code Examples section "Expanded rust-feature.md Target Pattern".

Per D-02: Match the depth and specificity of `crate-discovery.md` and `nix-maintenance.md`.
  </action>
  <verify>
    <automated>grep -n "## Goals\|## Workflow\|## Feature Checklist\|## Guardrails" .agent/skills/rust-feature.md | wc -l | xargs -I {} test {} -ge 4</automated>
    <automated>wc -l .agent/skills/rust-feature.md | awk '{print $1}' | xargs -I {} test {} -ge 80</automated>
    <automated>grep -c "just check\|just test" .agent/skills/rust-feature.md | xargs -I {} test {} -ge 2</automated>
    <automated>grep -c "## Guardrails" .agent/skills/rust-feature.md | xargs -I {} test {} -eq 1</automated>
  </verify>
  <done>
rust-feature.md expanded to ~80+ lines with:
- Goals section with 4-5 objectives
- Phased workflow with concrete commands
- Feature Checklist with Scope/Testing/Quality/Documentation categories
- Guardrails section with 5-6 items
- All commands reference justfile verification commands
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expand bugfix.md skill to polished pattern</name>
  <files>.agent/skills/bugfix.md</files>
  <read_first>
    - .agent/skills/bugfix.md (current content to expand)
    - .agent/skills/crate-discovery.md (reference pattern for structure)
    - .agent/skills/nix-maintenance.md (reference pattern for commands/checklists)
    - justfile (verification commands to reference)
    - AGENTS.md (verification contract for bug validation)
  </read_first>
  <action>
Expand `.agent/skills/bugfix.md` from ~16 lines to ~70+ lines following the polished skill pattern from D-02 and RESEARCH.md.

The expanded skill must include:

1. **Goals section** (expand from implicit to explicit 4 objectives):
   - Fix root causes, not symptoms.
   - Add regression tests when practical.
   - Preserve minimal change footprint.
   - Maintain code quality under pressure.

2. **Workflow section** structured into phases:
   - **Reproduction Phase**: Reproduce the issue (cargo test <test_name>), document expected vs actual behavior, confirm consistent reproduction
   - **Investigation Phase**: Identify smallest plausible root cause (RUST_BACKTRACE=1), verify root cause with minimal fix candidate
   - **Fix Phase**: Apply the fix (cargo check --all-targets), verify fix with narrow test (cargo test <test_name>), run full verification (just check && just test)

3. **Each workflow step must include**:
   - Concrete command with expected output
   - **What this checks/proves:** or **What this proves:** annotation
   - **Must:** success criteria where applicable

4. **Bugfix Checklist section** with categories:
   - Investigation checklist (issue reproduced, root cause identified, root cause documented)
   - Fix Quality checklist (change is minimal, addresses root cause, no opportunistic refactors, just check passes, just test passes)
   - Testing checklist (regression test added when practical, regression test would have caught bug, existing tests pass)
   - Documentation checklist (fix explained in commit message, related issue linked)

5. **Guardrails section** (expand from 2 to 5 items):
   - Avoid opportunistic refactors during a bugfix.
   - Do not remove failing tests to make CI pass.
   - Do not mask symptoms—fix root causes.
   - Do not expand scope—keep fix minimal.
   - Do not skip reproduction step—verify the bug is real.

Follow the exact structure from RESEARCH.md Code Examples section "Expanded bugfix.md Target Pattern".

Per D-02: Match the depth and specificity of `crate-discovery.md` and `nix-maintenance.md`.
  </action>
  <verify>
    <automated>grep -n "## Goals\|## Workflow\|## Bugfix Checklist\|## Guardrails" .agent/skills/bugfix.md | wc -l | xargs -I {} test {} -ge 4</automated>
    <automated>wc -l .agent/skills/bugfix.md | awk '{print $1}' | xargs -I {} test {} -ge 70</automated>
    <automated>grep -c "just check\|just test" .agent/skills/bugfix.md | xargs -I {} test {} -ge 2</automated>
    <automated>grep -c "## Guardrails" .agent/skills/bugfix.md | xargs -I {} test {} -eq 1</automated>
  </verify>
  <done>
bugfix.md expanded to ~70+ lines with:
- Goals section with 4 objectives
- Phased workflow (Reproduction/Investigation/Fix phases)
- Bugfix Checklist with Investigation/Fix Quality/Testing/Documentation categories
- Guardrails section with 5 items
- All commands reference justfile verification commands
  </done>
</task>

</tasks>

<verification>
## Plan Verification

After completing both tasks, verify:

1. **File structure matches polished pattern:**
   ```bash
   grep -n "## Goals\|## Workflow\|## Checklist\|## Guardrails" .agent/skills/rust-feature.md .agent/skills/bugfix.md
   ```

2. **Line counts meet minimums:**
   ```bash
   wc -l .agent/skills/rust-feature.md .agent/skills/bugfix.md
   # rust-feature.md >= 80 lines
   # bugfix.md >= 70 lines
   ```

3. **Verification commands referenced:**
   ```bash
   grep -c "just check\|just test" .agent/skills/rust-feature.md .agent/skills/bugfix.md
   # Each file >= 2 references
   ```

4. **Guardrails section present:**
   ```bash
   grep -c "## Guardrails" .agent/skills/rust-feature.md .agent/skills/bugfix.md
   # Each file == 1
   ```
</verification>

<success_criteria>
- Both skill files expanded to match polished skill structure
- Phased workflows with concrete commands and verification criteria
- Checklists with multiple categories
- Guardrails expanded to 5+ items each
- All verification commands reference existing justfile commands
- Files follow RESEARCH.md pattern examples exactly
</success_criteria>

<output>
After completion, create `.planning/phases/03-team-workflow-refinement/03-01-SUMMARY.md`
</output>