---
phase: 03-team-workflow-refinement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: true
requirements: [D-01]
user_setup: []

must_haves:
  truths:
    - "New contributors can follow documented development setup steps"
    - "Contributors know commit format requirements and PR process"
    - "Release workflow is documented with explicit version bump and tag steps"
    - "PR template provides consistent checklist for reviewers"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Contribution and release workflow guidance"
      contains: "## Development Setup"
      contains_2: "## Commit Format"
      contains_3: "## Release Workflow"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "PR description checklist"
      contains: "## Description"
      contains_2: "## Checklist"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "justfile"
      via: "Development workflow commands"
      pattern: "just (check|test|doctor|dev)"
    - from: ".github/PULL_REQUEST_TEMPLATE.md"
      to: "AGENTS.md"
      via: "Verification contract reference"
      pattern: "just check|just test"
---

<objective>
Create `CONTRIBUTING.md` and `.github/PULL_REQUEST_TEMPLATE.md` to establish team contribution and release workflows.

Purpose: Provide team-facing documentation for development setup, commit format, PR process, and release workflow aligned with existing repo commands and CI.

Output: Two new documentation files following proven open-source patterns from RESEARCH.md.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-team-workflow-refinement/03-CONTEXT.md
@.planning/phases/03-team-workflow-refinement/03-RESEARCH.md

<!-- Verification contract reference -->
@AGENTS.md

<!-- Workflow commands reference -->
@justfile

<!-- CI workflow to align with -->
@.github/workflows/ci.yml

<!-- Existing dev workflow to extend -->
@docs/dev-workflow.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From .github/workflows/ci.yml (CI commands to document):
```yaml
- name: Run flake checks
  run: nix flake check
- name: Run project checks inside dev shell
  run: nix develop --command just ci
```

From justfile (team commands):
- `just dev` - cargo run
- `just check` - fmt --check, clippy -D warnings, check
- `just test` - nextest run --all-features
- `just ci` - check + test
- `just doctor` - version checks

From docs/dev-workflow.md (existing setup):
- Day 0: Install Nix/direnv, clone, `direnv allow`, `just doctor`
- Day 1: Read AGENTS.md, README.md, planning docs
- Ongoing: `just dev`, `just watch`, keep docs current

From AGENTS.md (verification contract):
```markdown
Before declaring work complete, run the narrowest relevant validation:
- `cargo fmt --all -- --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo check --all-targets`
- `cargo test --all-features` or `cargo nextest run --all-features`
- `nix flake check` when Nix files change materially
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create CONTRIBUTING.md with dev setup, commit format, PR process, and release workflow</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - .planning/phases/03-team-workflow-refinement/03-RESEARCH.md (CONTRIBUTING.md pattern example)
    - AGENTS.md (verification contract)
    - justfile (team commands)
    - .github/workflows/ci.yml (CI workflow)
    - docs/dev-workflow.md (existing setup steps)
  </read_first>
  <action>
Create `CONTRIBUTING.md` at repository root following the pattern from RESEARCH.md "CONTRIBUTING.md Pattern" section.

The file must include these sections:

1. **Development Setup** section:
   - Install Nix and enable flakes
   - Install `direnv` and allow the repo
   - Run `direnv allow`
   - Run `just doctor` to verify environment
   - Reference: docs/dev-workflow.md Day 0 steps

2. **Development Workflow** section:
   - Use `just dev` for the default local run path
   - Use `just watch` during rapid iteration
   - Keep documentation and planning artifacts current when intent changes
   - Reference: docs/dev-workflow.md Day 1 and Ongoing steps

3. **Code Style** section:
   - We use `rustfmt` and `clippy` for code quality
   - Run `just check` before submitting PRs
   - Reference: AGENTS.md verification contract
   - Include exact command: `just check  # Runs fmt --check and clippy with -D warnings`

4. **Commit Format** section:
   - We follow conventional commits for clear history and changelog generation
   - List examples: `feat:`, `fix:`, `docs:`, `refactor:`, `test:`
   - Reference: RESEARCH.md commit format guidance

5. **Pull Request Process** section:
   - Draft PRs welcome for work inprogress
   - Describe changes and why in PR description
   - Link issues with "Closes #123" or "Fixes #456"
   - Run verification: `just check` and `just test` locally
   - CI must pass before merge
   - Reference: .github/workflows/ci.yml

6. **Release Workflow** section:
   - Releases follow semantic versioning (semver.org)
   - Steps:
     1. Update `Cargo.toml` version
     2. Ensure CI passes: `nix develop --command just ci`
     3. Tag the release: `git tag v0.1.0`
     4. Push the tag: `git push --tags`
     5. CI publishes artifacts automatically
   - Reference: CI workflow and Nix commands from AGENTS.md

7. **Code of Conduct** section:
   - We are all living beings, and what is most important is that we respect each other and work together. If you cannot uphold this simple standard, then your contributions are not welcome.
   - Reference: Standard open-source pattern

Format: Use standard Markdown with clear section headers (##). Keep sections concise but actionable.

Per D-01 and RESEARCH.md: Align documentation with existing repo commands and CI semantics.
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md</automated>
    <automated>grep -n "## Development Setup\|## Development Workflow\|## Code Style\|## Commit Format\|## Pull Request Process\|## Release Workflow\|## Code of Conduct" CONTRIBUTING.md | wc -l | xargs -I {} test {} -eq 7</automated>
    <automated>grep -c "just check\|just test\|just doctor\|just ci" CONTRIBUTING.md | xargs -I {} test {} -ge 4</automated>
    <automated>grep -c "nix develop\|nix flake check" CONTRIBUTING.md | xargs -I {} test {} -ge 2</automated>
    <automated>grep -c "semver\|semantic versioning\|conventional commits" CONTRIBUTING.md | xargs -I {} test {} -ge 2</automated>
  </verify>
  <done>
CONTRIBUTING.md created with all 7 required sections:
- Development Setup with just doctor
- Development Workflow with just dev/watch
- Code Style with just check
- Commit Format with conventional commits
- Pull Request Process with CI reference
- Release Workflow with version bump and tag steps
- Code of Conduct
All commands reference existing justfile and CI workflow.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create .github/PULL_REQUEST_TEMPLATE.md with checklist</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>
    - .planning/phases/03-team-workflow-refinement/03-RESEARCH.md (PR template pattern example)
    - AGENTS.md (verification contract)
    - justfile (verification commands)
    - .github/workflows/ci.yml (CI commands)
  </read_first>
  <action>
Create `.github/PULL_REQUEST_TEMPLATE.md` following the pattern from RESEARCH.md "PR template structure" section.

The file must include these sections:

1. **## Description** section:
   - Comment: "<!-- What changed and why -->"
   - Empty space for contributor to fill in

2. **## Type of Change** section:
   - Checklist with:
     - `[ ] New feature`
     - `[ ] Bug fix`
     - `[ ] Refactoring`
     - `[ ] Documentation`
     - `[ ] Infrastructure`

3. **## Changes Made** section:
   - Comment: "<!-- List specific changes -->"
   - Empty space for contributor to list changes

4. **## How to Test** section:
   - Numbered steps placeholder
   - Include example: `1. Run \`just check && just test\``
   - Reference: AGENTS.md verification contract

5. **## Checklist** section:
   - `[ ] Code follows style guidelines`
   - `[ ] Self-reviewed code`
   - `[ ] Tests added/updated`
   - `[ ] Existing tests pass (`just test`)`
   - `[ ] Lints pass (`just check`)`
   - `[ ] Documentation updated`

6. **## Related Issues** section:
   - Comment: "<!-- Closes #123, Fixes #456 -->"
   - Empty space for issue links

Format: Use standard Markdown with clear section headers (##). Keep sections concise.

Per RESEARCH.md: PR template should ensure consistency and faster reviews through pre-populated checklist.
  </action>
  <verify>
    <automated>test -f .github/PULL_REQUEST_TEMPLATE.md</automated>
    <automated>grep -n "## Description\|## Type of Change\|## Changes Made\|## How to Test\|## Checklist\|## Related Issues" .github/PULL_REQUEST_TEMPLATE.md | wc -l | xargs -I {} test {} -eq 6</automated>
    <automated>grep -c "just check\|just test" .github/PULL_REQUEST_TEMPLATE.md | xargs -I {} test {} -ge 2</automated>
    <automated>grep -c "\[ \]" .github/PULL_REQUEST_TEMPLATE.md | xargs -I {} test {} -ge 5</automated>
  </verify>
  <done>
.github/PULL_REQUEST_TEMPLATE.md created with all 6 required sections:
- Description placeholder
- Type of Change checklist
- Changes Made placeholder
- How to Test with example commands
- Checklist referencing just check and just test
- Related Issues placeholder
Template ensures PR consistency and verification before merge.
  </done>
</task>

</tasks>

<verification>
## Plan Verification

After completing both tasks, verify:

1. **CONTRIBUTING.md structure:**
   ```bash
   grep -n "## Development Setup\|## Development Workflow\|## Code Style\|## Commit Format\|## Pull Request Process\|## Release Workflow\|## Code of Conduct" CONTRIBUTING.md
   # Exactly 7 section headers
   ```

2. **PR template structure:**
   ```bash
   grep -n "## Description\|## Type of Change\|## Changes Made\|## How to Test\|## Checklist\|## Related Issues" .github/PULL_REQUEST_TEMPLATE.md
   # Exactly 6 section headers
   ```

3. **Command alignment with justfile:**
   ```bash
   grep -c "just check\|just test\|just doctor\|just ci" CONTRIBUTING.md .github/PULL_REQUEST_TEMPLATE.md
   # CONTRIBUTING.md >= 4, PR template >= 2
   ```

4. **Alignment with CI workflow:**
   ```bash
   grep -c "nix develop\|nix flake check" CONTRIBUTING.md
   # >= 2 references
   ```

5. **Manual verification:**
   - Read CONTRIBUTING.md end-to-end and confirm documented commands matchjustfile andci.yml
   - Read PR template end-to-end and confirm checklist items match AGENTS.md verification contract
</verification>

<success_criteria>
- CONTRIBUTING.md created with all 7 required sections
- PR template created with all 6 required sections
- All documented commands exist in justfile
- Release workflow aligns with CI workflow
- Verification checklist matches AGENTS.md verification contract
- Conventional commits guidance included
</success_criteria>

<output>
After completion, create `.planning/phases/03-team-workflow-refinement/03-02-SUMMARY.md`
</output>