---
phase: 02-rust-app-specializations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .agent/skills/nix-maintenance.md
autonomous: true
requirements: [D-05, D-06]
user_setup: []

must_haves:
  truths:
    - "Agent can validate Nix changes with nix flake check"
    - "Agent can validate dev shell by running just commands inside nix develop"
    - "Agent knows concrete pass/fail criteria for Nix validation"
    - "Agent knows to review flake.lock changes before committing"
  artifacts:
    - path: ".agent/skills/nix-maintenance.md"
      provides: "Nix validation workflow with concrete commands"
      min_lines: 45
      contains:
        - "nix flake check"
        - "nix develop --command"
        - "pass/fail"
        - "intentional"
  key_links:
    - from: ".agent/skills/nix-maintenance.md"
      to: "nix commands"
      via: "validation loop"
      pattern: "nix (flake check|develop --command)"
    - from: ".agent/skills/nix-maintenance.md"
      to: "just entrypoints"
      via: "workflow validation"
      pattern: "just (check|test|ci)"
---

<objective>
Enhance the nix-maintenance skill with concrete validation loops that satisfy Decisions D-05 and D-06.

Purpose: Agents need explicit command sequences with pass/fail criteria—not vague advice—to validate Nix changes correctly.

Output: Enhanced `.agent/skills/nix-maintenance.md` with concrete `nix flake check` and `nix develop --command` validation loops.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rust-app-specializations/02-CONTEXT.md
@.planning/phases/02-rust-app-specializations/02-RESEARCH.md
@.agent/skills/nix-maintenance.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance nix-maintenance skill with concrete validation loops</name>
  <files>.agent/skills/nix-maintenance.md</files>
  <read_first>
    - .agent/skills/nix-maintenance.md (current skill to enhance)
    - .planning/phases/02-rust-app-specializations/02-RESEARCH.md (validation architecture, Nix patterns)
    - .planning/phases/02-rust-app-specializations/02-CONTEXT.md (locked decisions D-05, D-06)
  </read_first>
  <action>
    Enhance `.agent/skills/nix-maintenance.md` with the following additions (per D-05, D-06):

    **1. Expand the Goals section** to emphasize concrete validation:
    ```markdown
    ## Goals

    - Keep `flake.nix`, `flake.lock`, dev shell behavior, and CI in sync.
    - Make Nix changes reviewable and reproducible.
    - Prove that both flake evaluation and repo entrypoints still work after the change.
    - Use concrete command sequences with pass/fail criteria—not vague advice.
    ```

    **2. Enhance the Workflow section** with explicit validation loops:
    ```markdown
    ## Workflow

    ### Before Editing
    1. Read the current state:
       ```bash
       cat flake.nix      # Understand current outputs and inputs
       cat flake.lock     # Check current input versions
       cat justfile       # Understand repo entrypoints
       ```
       Know what each output does before changing it.

    ### After Editing

    2. **Step 1: Validate flake evaluation**
       ```bash
       nix flake check
       ```
       **Must:** Exit with code 0 (no evaluation errors)

       **What this checks:**
       - Flake evaluates correctly
       - All outputs are valid
       - No circular dependencies

    3. **Step 2: Validate dev shell from inside**
       ```bash
       nix develop --command bash -lc 'just check && just test && just ci'
       ```
       **Must:** All commands complete without error

       **What this proves:**
       - Dev shell provides all required tools
       - repo commands work in the Nix environment
       - CI workflow is reproducible locally

    4. **Step 3: Review lock file changes**
       ```bash
       git diff flake.lock
       ```
       **Must:** Intentional dependency changes only

       **What to check:**
       - Input hashes changed for intentional updates
       - No surprise transitive dependency bumps
       - Lock file matches flake.nix edits

    ### When to Run Which Validation

    | Change Type | `nix flake check` | `nix develop --command` |
    |-------------|-------------------|-------------------------|
    | flake.nix edit | ✓ Always | ✓ Always |
    | flake.lock update | ✓ Always | ✓ If inputs changed |
    | Dev shell tooling | ✓ Always | ✓ Always |
    | Output names | ✓ Always | ✗ Optional |
    | Comments/formatting | ✗ Optional | ✗ Optional |
    ```

    **3. Replace the Verification checklist section** with concrete commands:
    ```markdown
    ## Verification Checklist

    After any meaningful Nix change:

    - [ ] `nix flake check` exits with code 0
    - [ ] `nix develop --command bash -lc 'just check && just test && just ci'` completes without error
    - [ ] `git diff flake.lock` shows only intentional changes
    - [ ] README documented new commands or tools (if workflow changed)
    - [ ] `.planning/STATE.md` updated (if workflow expectations changed)
    ```

    **4. Update the Guardrails section** to be more explicit:
    ```markdown
    ## Guardrails

    - Do not add duplicate ways to run the same workflow without a clear need.
    - Do not bypass Nix for convenience unless the project explicitly wants that tradeoff.
    - Do not update Nix inputs casually; keep dependency movement intentional and reviewable.
    - Do not change shell tooling without checking whether README and CI still describe the repo accurately.
    - Do not skip `nix flake check` after flake.nix edits.
    - Do not assume evaluation success means dev shell works—validate from inside `nix develop`.
    ```
  </action>
  <verify>
    <automated>grep -n "nix flake check" .agent/skills/nix-maintenance.md </automated>
    <automated>grep -n "nix develop --command" .agent/skills/nix-maintenance.md </automated>
    <automated>grep -n "Exit with code 0\|completes without error" .agent/skills/nix-maintenance.md </automated>
    <automated>grep -n "When to Run Which Validation" .agent/skills/nix-maintenance.md </automated>
  </verify>
  <acceptance_criteria>
    - File `.agent/skills/nix-maintenance.md` contains explicit `nix flake check` command with pass criteria
    - File contains explicit `nix develop --command` command with pass criteria
    - File contains "Must:" statements defining success criteria
    - File contains decision table for when to run which validation
    - File contains verification checklist with concrete commands
    - File maintains concise, actionable tone consistent with existing skills
    - File is at least 45 lines (current is 32, enhancements should add substantial content)
  </acceptance_criteria>
  <done>
    Enhanced nix-maintenance skill with concrete validation loops covering nix flake check, nix develop --command, and explicit pass/fail criteria.
  </done>
</task>

</tasks>

<verification>
## Plan 02 Verification

1. **Skill content verification:**
   ```bash
   grep -n "nix flake check" .agent/skills/nix-maintenance.md
   grep -n "nix develop --command" .agent/skills/nix-maintenance.md
   grep -n "Exit with code 0" .agent/skills/nix-maintenance.md
   grep -n "When to Run Which Validation" .agent/skills/nix-maintenance.md
   ```

2. **Structure verification:**
   ```bash
   wc -l .agent/skills/nix-maintenance.md  # Should be >= 45 lines
   ```

3. **Human review:** Read the enhanced skill end-to-end and confirm:
   - Commands are specific with exact flags and expected outcomes
   - Pass/fail criteria are explicit
   - Decision table clarifies when to run each validation
</verification>

<success_criteria>
- [ ] Skill contains explicit `nix flake check` command
- [ ] Skill contains explicit `nix develop --command` command
- [ ] Skill contains explicit pass/fail criteria ("Must:" statements)
- [ ] Skill contains decision table for validation timing
- [ ] Skill contains verification checklist with concrete commands
- [ ] Skill maintains concise, actionable tone
- [ ] File is at least 45 lines
</success_criteria>

<output>
After completion, create `.planning/phases/02-rust-app-specializations/02-02-SUMMARY.md`
</output>