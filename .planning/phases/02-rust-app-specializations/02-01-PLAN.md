---
phase: 02-rust-app-specializations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agent/skills/crate-discovery.md
autonomous: true
requirements: [D-02, D-03, D-04]
user_setup: []

must_haves:
  truths:
    - "Agent can discover candidate crates with cargo search"
    - "Agent can inspect crate metadata with cargo info"
    - "Agent evaluates crates on license, maintenance, docs, and ecosystem fit"
    - "Agent knows to use Context7 when docs.rs is insufficient"
    - "Agent documents why a crate was chosen over alternatives"
  artifacts:
    - path: ".agent/skills/crate-discovery.md"
      provides: "Crate discovery and evaluation workflow"
      min_lines: 50
      contains:
        - "cargo search"
        - "cargo info"
        - "Context7"
        - "license"
        - "maintenance"
        - "documentation"
        - "ecosystem"
  key_links:
    - from: ".agent/skills/crate-discovery.md"
      to: "cargo commands"
      via: "workflow step"
      pattern: "cargo (search|info)"
    - from: ".agent/skills/crate-discovery.md"
      to: "Context7"
      via: "doc lookup"
      pattern: "Context7"
---

<objective>
Enhance the crate-discovery skill with a comprehensive evaluation workflow that satisfies Decisions D-02, D-03, and D-04.

Purpose: Agents need clear guidance for selecting crates based on license, maintenance signals, documentation quality, and ecosystem fit—not just download counts.

Output: Enhanced `.agent/skills/crate-discovery.md` with structured discovery and evaluation checklist.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rust-app-specializations/02-CONTEXT.md
@.planning/phases/02-rust-app-specializations/02-RESEARCH.md
@.agent/skills/crate-discovery.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance crate-discovery skill with evaluation workflow</name>
  <files>.agent/skills/crate-discovery.md</files>
  <read_first>
    - .agent/skills/crate-discovery.md (current skill to enhance)
    - .planning/phases/02-rust-app-specializations/02-RESEARCH.md (validation architecture, evaluation patterns)
    - .planning/phases/02-rust-app-specializations/02-CONTEXT.md (locked decisions D-02, D-03, D-04)
  </read_first>
  <action>
    Enhance `.agent/skills/crate-discovery.md` with the following additions (per D-02, D-03, D-04):

    **1. Expand the Goals section** to explicitly mention:
    - Start with lightweight discovery before adding a dependency
    - Prefer maintained, well-documented crates with compatible licenses
    - Evaluate crates on license, maintenance, documentation quality, and ecosystem fit
    - Use Context7 for version-specific API documentation when needed

    **2. Enhance the Workflow section** with concrete steps:
    ```markdown
    ## Workflow

    ### Discovery Phase
    1. Run initial search:
       ```bash
       cargo search --limit 20 <problem-domain>
       ```
       Review names, descriptions, and download counts as signals—not decisions.

    2. For promising candidates, inspect metadata:
       ```bash
       cargo info <crate>
       ```
       Note: license, latest version, repository, documentation link.

    ### Evaluation Phase
    3. Check repository freshness:
       - Visit repository URL from `cargo info` output
       - Check last commit date, open issues, PR response time

    4. Verify documentation quality:
       - Open docs.rs/<crate>
       - Check for examples, getting-started guide
       - Use Context7 if docs.rs is incomplete or version differs

    5. Review dependency footprint:
       ```bash
       cargo add <crate> --dry-run
       cargo tree --duplicates  # After dry-run to see transitive deps
       ```

    ### Decision Phase
    6. Document choice with:
       - Why this crate over alternatives
       - License confirmed compatible
       - Maintenance status confirmed active
       - No smaller alternative found
    ```

    **3. Replace the Evaluation checklist section** with a comprehensive checklist:
    ```markdown
    ## Evaluation Checklist

    Before recommending `<crate>` for adoption:

    ### License Compatibility
    - [ ] License is OSI-approved and permissive (MIT, Apache-2.0, BSD, ISC)
    - [ ] All transitive dependencies have compatible licenses
    - [ ] Run: `cargo info <crate>` to verify license field

    ### Maintenance Signals
    - [ ] Repository has commits in last 6 months (check GitHub/GitLab)
    - [ ] Issues/PRs are actively triaged
    - [ ] Version follows semantic versioning
    - [ ] Recent releases exist (check `cargo info <crate>`)

    ### Documentation Quality
    - [ ] README explains purpose and basic usage
    - [ ] API docs on docs.rs are navigable
    - [ ] Examples demonstrate common use cases
    - [ ] Use Context7 for version-specific API if docs.rs is insufficient

    ### Ecosystem Fit
    - [ ] Crate solves the specific problem (not over-featured)
    - [ ] Transitive dependency count is reasonable
    - [ ] No duplicate versions of same crate in dependency tree
    - [ ] Run: `cargo tree --duplicates` after adding to check
    ```

    **4. Update the Guardrails section** to be more explicit:
    ```markdown
    ## Guardrails

    - Do not add a new crate before checking whether the standard library or an existing dependency already solves the problem.
    - Do not pick a crate by download counts alone—evaluate maintenance signals, documentation, and ecosystem fit.
    - Do not recommend crates with unclear licensing or obviously stale maintenance without calling out the risk.
    - Do not skip documentation lookup when the crate API or ecosystem fit is uncertain.
    - Do not assume MIT/Apache—always verify the license field with `cargo info <crate>`.
    ```

    **5. Keep the existing tone and conciseness** (short sections, actionable steps) while adding depth to match the research patterns.

    Reference the RESEARCH.md "Architecture Patterns/Pattern 1: Structured Crate Evaluation" and "Code Examples/Example: Crate Discovery Workflow" for exact wording patterns.
  </action>
  <verify>
    <automated>grep -n "cargo search\|cargo info\|Context7\|license\|maintenance\|ecosystem" .agent/skills/crate-discovery.md | wc -l | grep -q "^[5-9]\|[1-9][0-9]" </automated>
    <automated>grep -n "Evaluation Checklist" .agent/skills/crate-discovery.md </automated>
    <automated>grep -n "Discovery Phase\|Evaluation Phase\|Decision Phase" .agent/skills/crate-discovery.md </automated>
  </verify>
  <acceptance_criteria>
    - File `.agent/skills/crate-discovery.md` contains "cargo search" workflow step
    - File contains "cargo info" workflow step
    - File contains Context7 reference for doc lookup
    - File contains structured "Evaluation Checklist" with License, Maintenance, Documentation Quality, Ecosystem Fit categories
    - File contains explicit guardrails against choosing by download counts alone
    - File maintains concise, actionable tone consistent with existing skills
    - File is at least 50 lines (current is 34, enhancements should add substantial content)
  </acceptance_criteria>
  <done>
    Enhanced crate-discovery skill with comprehensive evaluation workflow covering cargo search, cargo info, Context7, and evaluation criteria (license, maintenance, docs, ecosystem fit).
  </done>
</task>

</tasks>

<verification>
## Plan 01 Verification

1. **Skill content verification:**
   ```bash
   grep -n "cargo search" .agent/skills/crate-discovery.md
   grep -n "cargo info" .agent/skills/crate-discovery.md
   grep -n "Context7" .agent/skills/crate-discovery.md
   grep -n "License\|Maintenance\|Documentation Quality\|Ecosystem Fit" .agent/skills/crate-discovery.md
   ```

2. **Structure verification:**
   ```bash
   wc -l .agent/skills/crate-discovery.md  # Should be >= 50 lines
   ```

3. **Human review:** Read the enhanced skill end-to-end and confirm:
   - A developer can execute the workflow without guessing next steps
   - The evaluation checklist covers common failure modes
   - Commands are specific (not vague)
</verification>

<success_criteria>
- [ ] Skill contains `cargo search` workflow step
- [ ] Skill contains `cargo info` workflow step
- [ ] Skill contains Context7 reference for documentation lookup
- [ ] Skill contains structured evaluation checklist with License, Maintenance, Documentation Quality, Ecosystem Fit categories
- [ ] Skill contains guardrails against choosing by download counts alone
- [ ] Skill maintains concise, actionable tone
- [ ] File is at least 50 lines
</success_criteria>

<output>
After completion, create `.planning/phases/02-rust-app-specializations/02-01-SUMMARY.md`
</output>