---
phase: 03-team-workflow-refinement
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - prompts/add-feature.md
  - prompts/debug-failure.md
autonomous: true
requirements: [D-01, D-03, D-04, D-05]
user_setup: []

must_haves:
  truths:
    - "Prompts remain concise entrypoints (under 25 lines)"
    - "Prompts explicitly point to skills for detailed workflow guidance"
    - "Prompts do not duplicate skill content"
    - "Prompts referenceproject context files correctly"
  artifacts:
    - path: "prompts/add-feature.md"
      provides: "Feature kickoff prompt"
      max_lines: 25
      contains: ".agent/skills/rust-feature.md"
      contains_2: ".planning/"
    - path: "prompts/debug-failure.md"
      provides: "Debug kickoff prompt"
      max_lines: 20
      contains: ".agent/skills/bugfix.md"
      contains_2: "root cause"
  key_links:
    - from: "prompts/add-feature.md"
      to: ".agent/skills/rust-feature.md"
      via: "Workflow guidance reference"
      pattern: "\.agent/skills/"
    - from: "prompts/debug-failure.md"
      to: ".agent/skills/bugfix.md"
      via: "Workflow guidance reference"
      pattern: "\.agent/skills/"
---

<objective>
Tailor `add-feature.md` and `debug-failure.md` prompts to real usage patterns while keeping them concise entrypoints that point to skills.

Purpose: Remove placeholder text and provide actionable guidance per D-03 and D-04, while ensuring prompts point to skills per D-05.

Output: Two updated prompt files (each under25 lines) with skill references and project context pointers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-team-workflow-refinement/03-CONTEXT.md
@.planning/phases/03-team-workflow-refinement/03-RESEARCH.md

<!-- Expanded skills (created in Plan 01) -->
@.agent/skills/rust-feature.md
@.agent/skills/bugfix.md

<!-- Current placeholder prompts to tailor -->
@prompts/add-feature.md
@prompts/debug-failure.md

<!-- Project context to reference -->
@AGENTS.md
</context>

<interfaces>
<!-- Key contracts from D-04 and D-05 -->

From D-04 (Prompt Structure):
- Prompts remain concise task entrypoints
- Stay under 25 lines
- Do NOT include detailed commands (belong in skill)
- Do NOT duplicate skill content

From D-05 (Skill Reference Pattern):
```markdown
# Prompt: [Action Name]

[One-line purpose statement].

Before editing, read [project context references].

Then:

1. [High-level step 1]
2. [High-level step 2]
3. [High-level step 3 with skill reference]: See `.agent/skills/[skill].md` for workflow.

[Reference to planning docs if applicable]
```

From RESEARCH.md (Prompt Warning Signs):
- Prompt exceeds 30 lines (TOO LONG)
- Prompt includes detailed commands (belongs in skill)
- Prompt duplicates content from `.agent/skills/` or `.planning/`

From existing prompts (current state):
- add-feature.md: 13 lines (placeholder)
- debug-failure.md: 11 lines (placeholder)

Target state:
- add-feature.md: ~20 lines (tailored)
- debug-failure.md: ~18 lines (tailored)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tailor add-feature.md prompt to real usage</name>
  <files>prompts/add-feature.md</files>
  <read_first>
    - prompts/add-feature.md (current placeholder to tailor)
    - .agent/skills/rust-feature.md (expanded skill to reference - created in Plan01)
    - .planning/phases/03-team-workflow-refinement/03-RESEARCH.md (prompt pattern from Code Examples section)
    - AGENTS.md (project context to reference)
    - README.md (project context to reference)
  </read_first>
  <action>
Update `prompts/add-feature.md` from generic placeholder to tailored prompt following the pattern from RESEARCH.md "Tailored Prompt Example (add-feature.md)".

The updated prompt must:

1. **Keep concise** (target: ~20 lines, max 25 lines):
   - One-line purpose statement
   - 5-6 high-level steps
   - Explicit skill reference
   - Planning context reference

2. **Content structure** (per RESEARCH.md example):
   ```markdown
   # Prompt: Add a feature

   Implement a new feature in this repository.

   Before editing, read `AGENTS.md`, `README.md`, and `.planning/PROJECT.md` for project context.

   Then:

   1. Summarize the feature boundary: what it does and why.
   2. Identify the narrowest module that can own the change.
   3. Implement the smallest complete version with tests.
   4. Run verification: `just check && just test`.
   5. Update documentation if workflow changed.

   **Workflow guidance:** See `.agent/skills/rust-feature.md` for the full feature workflow with verification checklist.

   **Planning context:** If this feature changes project intent, update `.planning/STATE.md`.
   ```

3. **Key elements to include**:
   - Purpose statement: "Implement a new feature in this repository."
   - Context pointers: AGENTS.md, README.md, .planning/PROJECT.md
   - High-level steps (not detailed commands - those are in the skill)
   - Skill reference: `.agent/skills/rust-feature.md`
   - Planning reference: `.planning/STATE.md` for intent changes

4. **Key elements to AVOID** (per D-04 and RESEARCH.md Pitfall 1):
   - Do NOT include detailed workflow commands (belong in skill)
   - Do NOT duplicate Feature Checklist content from skill
   - Do NOT exceed 25 lines
   - Do NOT create a mini-skill document

Per D-03: Tailor prompts to real usage patterns instead of generic placeholder text.
Per D-04: Prompts remain concise task entrypoints, not full duplicated workflows.
Per D-05: Prompts explicitly point to skills for detailed workflow guidance.
  </action>
  <verify>
    <automated>wc -l prompts/add-feature.md | awk '{print $1}' | xargs -I {} test {} -le 25</automated>
    <automated>grep -c "\.agent/skills/rust-feature\.md" prompts/add-feature.md | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "\.planning/PROJECT\.md\|\.planning/STATE\.md" prompts/add-feature.md | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "just check\|just test" prompts/add-feature.md | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "## Goals\|## Workflow\|## Checklist" prompts/add-feature.md | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
prompts/add-feature.md tailored from generic placeholder to real usage:
- One-line purpose statement
- 5-6 high-level steps
- Explicit skill reference: `.agent/skills/rust-feature.md`
- Planning context reference: `.planning/PROJECT.md` and `.planning/STATE.md`
- Under 25 lines (concise entrypoint per D-04)
- No duplicated workflow content (per D-05)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tailor debug-failure.md prompt to real usage</name>
  <files>prompts/debug-failure.md</files>
  <read_first>
    - prompts/debug-failure.md (current placeholder to tailor)
    - .agent/skills/bugfix.md (expanded skill to reference - created in Plan01)
    - .planning/phases/03-team-workflow-refinement/03-RESEARCH.md (prompt pattern from Code Examples section)
    - AGENTS.md (verification contract to reference)
  </read_first>
  <action>
Update `prompts/debug-failure.md` from generic placeholder to tailored prompt following the pattern from RESEARCH.md "Tailored Prompt Example (debug-failure.md)".

The updated prompt must:

1. **Keep concise** (target: ~18 lines, max20 lines):
   - One-line purpose statement
   - 5 high-level steps
   - Explicit skill reference
   - Quality bar reminder

2. **Content structure** (per RESEARCH.md example):
   ```markdown
   # Prompt: Debug a failure

   Investigate a failing command, test, or CI job in this repository.

   Before changing code, reproduce the issue:

   1. Run the failing command or test locally.
   2. Capture the error message and backtrace.
   3. Identify the smallest plausible root cause.
   4. Apply a minimal fix that addresses the root cause.
   5. Run verification: `just check && just test`.

   **Workflow guidance:** See `.agent/skills/bugfix.md` for the full debugging workflow with checklist.

   **Quality bar:** Do not remove failing tests. Fix root causes, not symptoms.
   ```

3. **Key elements to include**:
   - Purpose statement: "Investigate a failing command, test, or CI job in this repository."
   - High-level steps focused on debugging workflow (not detailed commands)
   - Skill reference: `.agent/skills/bugfix.md`
   - Quality bar: Root causes, not symptoms; don't remove failing tests

4. **Key elements to AVOID** (per D-04 and RESEARCH.md Pitfall 1):
   - Do NOT include detailed debugging commands (belong in skill)
   - Do NOT duplicate Bugfix Checklist content from skill
   - Do NOT exceed 20 lines
   - Do NOT create a mini-skill document

Per D-03: Tailor prompts to real usage patterns instead of generic placeholder text.
Per D-04: Prompts remain concise task entrypoints, not full duplicated workflows.
Per D-05: Prompts explicitly point to skills for detailed workflow guidance.
  </action>
  <verify>
    <automated>wc -l prompts/debug-failure.md | awk '{print $1}' | xargs -I {} test {} -le 20</automated>
    <automated>grep -c "\.agent/skills/bugfix\.md" prompts/debug-failure.md | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "root cause" prompts/debug-failure.md | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "just check\|just test" prompts/debug-failure.md | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "## Goals\|## Workflow\|## Checklist" prompts/debug-failure.md | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
prompts/debug-failure.md tailored from generic placeholder to real usage:
- One-line purpose statement
- 5 high-level debugging steps
- Explicit skill reference: `.agent/skills/bugfix.md`
- Quality bar reminder: root causes, not symptoms
- Under 20 lines (concise entrypoint per D-04)
- No duplicated workflow content (per D-05)
  </done>
</task>

</tasks>

<verification>
## Plan Verification

After completing both tasks, verify:

1. **Prompt brevity:**
   ```bash
   wc -l prompts/add-feature.md prompts/debug-failure.md
   # add-feature.md <= 25 lines
   # debug-failure.md <= 20 lines
   ```

2. **Skill references present:**
   ```bash
   grep -c "\.agent/skills/rust-feature\.md" prompts/add-feature.md
   # >= 1
   grep -c "\.agent/skills/bugfix\.md" prompts/debug-failure.md
   # >= 1
   ```

3. **No workflow duplication:**
   ```bash
   grep -c "## Goals\|## Workflow\|## Checklist" prompts/add-feature.md prompts/debug-failure.md
   # Both == 0 (no duplicated skill sections)
   ```

4. **Planning context reference:**
   ```bash
   grep -c "\.planning/" prompts/add-feature.md
   # >= 1
   ```

5. **Manual verification:**
   - Read both prompts end-to-end and confirm they are concise entrypoints
   - Confirm prompts do NOT duplicate skill content
   - Confirm prompts point to skills for detailed workflows
</verification>

<success_criteria>
- Both prompts under their line limits (25/20)
- Both prompts reference their corresponding skill
- No duplicated workflow content from skills
- Prompts remain concise entrypoints per D-04
- Prompts point to skills per D-05
- Planning context referenced where appropriate
</success_criteria>

<output>
After completion, create `.planning/phases/03-team-workflow-refinement/03-03-SUMMARY.md`
</output>